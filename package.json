{
  "name": "engineer-daily",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "concurrently \"npm run dev -w apps/worker\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w apps/worker && npm run build -w apps/web",
    "deploy": "npm run deploy -w apps/worker && npm run deploy -w apps/web",
    "next-concepts": "cd scripts && npm run next-concepts"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
